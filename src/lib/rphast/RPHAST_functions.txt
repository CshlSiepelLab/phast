phast.init()  #load phast libraries

msa.read(fname, format="FASTA", maxseq=100)   
#maxseq is maximum number of species allowed, used 
#  to help R with allocating memory for storing species names

msa.write(fname, msa, format=attr(msa,"format"))

msa.free(msa)

tm.read(fname)

tm.scale(treemod, scale)

tm.write(fname, treemod)

tm.free(tm)

tm.generateMSA(treemod, nsites, maxsp=100)
